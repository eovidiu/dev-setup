# Isolated Development Environment Configuration Template
# Copy this file to .env and customize for your setup

# Environment name (alphanumeric + hyphens, max 64 chars)
ENV_NAME=claude-dev

# Base container image
# Use the pre-built image from GitHub Container Registry for faster startup
# Or use 'ubuntu:22.04' to build from scratch
BASE_IMAGE=ghcr.io/eovidiu/claude-sandbox:latest

# Language runtime versions
NODEJS_VERSION=lts/*
PYTHON_VERSION=3.11

# Working directory inside container
WORK_DIR=/workspace

# Host mounts (comma-separated: HOST_PATH:CONTAINER_PATH:MODE)
# MODE: rw (read-write) or ro (read-only)
# Example: /Users/me/projects:/workspace:rw,/Users/me/shared:/shared:ro
HOST_MOUNTS=/Users/[YOUR_USERNAME]/[YOUR_WORK_FOLDER]/:/workspace:rw

# Port mappings (comma-separated: HOST_PORT:CONTAINER_PORT)
# Example: 3000:3000,8000:8000
PORTS=3000:3000,8000:8000

# Data persistence
# Set to true to keep volumes after environment destruction
PERSIST_DATA=false

# Non-secret environment variables (comma-separated: KEY=VALUE)
# DO NOT put secrets here! Use --secret flag when starting environment
# Example: NODE_ENV=development,LOG_LEVEL=debug
ENVIRONMENT_VARS=NODE_ENV=development,PYTHON_ENV=development
